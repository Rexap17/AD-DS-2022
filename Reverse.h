#ifndef REVERSE_H
#define REVERSE_H

#include <string>

class Reverse{
    public:

    Reverse();
    int reverseDigit(int);
    std::string reverseString(std::string);

    private:
    
};
#endif

